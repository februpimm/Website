{
  "name": "website",
  "version": "1.0.0",
  "description": "LogLens Website with Authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "set NODE_ENV=development && node server.js",
    "prod": "set NODE_ENV=production && node server.js",
    "deploy": "set NODE_ENV=production && set DOMAIN=yourdomain.com && node server.js",
    "pm2-start": "pm2 start ecosystem.config.js",
    "pm2-stop": "pm2 stop ecosystem.config.js",
    "pm2-restart": "pm2 restart ecosystem.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["express", "authentication", "logs", "analysis", "security"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "helmet": "^7.1.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
